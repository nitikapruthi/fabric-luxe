@extends('layouts.admin') @section('content')<header class="admin-header"><div><p>CATALOGUE / COLLECTIONS</p><h1>{{ $category->exists?'Edit collection':'New collection' }}</h1></div><a href="{{ route('admin.categories.index') }}">← Back</a></header><form class="admin-form" method="POST" action="{{ $category->exists?route('admin.categories.update',$category):route('admin.categories.store') }}">@csrf @if($category->exists)@method('PUT')@endif<label>Name<input name="name" value="{{ old('name',$category->name) }}" required></label><label>Parent collection<select name="parent_id"><option value="">None — top-level category</option>@foreach($parents as $parent)<option value="{{ $parent->id }}" @selected(old('parent_id',$category->parent_id)==$parent->id)>{{ str_repeat('— ', $parent->level()-1) }}{{ $parent->name }} (Level {{ $parent->level() }})</option>@endforeach</select><small>Maximum hierarchy: Category → Subcategory → Sub-subcategory.</small></label><label>Image URL<input name="image" value="{{ old('image',$category->image) }}" placeholder="https://..."></label><label>Description<textarea name="description">{{ old('description',$category->description) }}</textarea></label><button class="primary">Save collection</button></form>@endsection
