@extends('layouts.admin') @section('content')<header class="admin-header"><div><p>CATALOGUE</p><h1>Collections</h1><small>Use a parent to create a category, subcategory or sub-subcategory.</small></div><a class="primary" href="{{ route('admin.categories.create') }}">+ Add collection</a></header><section class="admin-section"><table><thead><tr><th>Name</th><th>Level</th><th>Parent</th><th>Products</th><th></th></tr></thead><tbody>@foreach($categories as $category)<tr><td><b>{{ str_repeat('— ', $category->level()-1) }}{{ $category->name }}</b></td><td>Level {{ $category->level() }}</td><td>{{ $category->parent?->name ?? '—' }}</td><td>{{ $category->products->count() }}</td><td class="actions"><a href="{{ route('admin.categories.edit',$category) }}">Edit</a><form method="POST" action="{{ route('admin.categories.destroy',$category) }}">@csrf @method('DELETE')<button onclick="return confirm('Remove this collection?')">Delete</button></form></td></tr>@endforeach</tbody></table></section>@endsection
