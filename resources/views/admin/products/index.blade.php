@extends('layouts.admin') @section('content')<header class="admin-header"><div><p>CATALOGUE</p><h1>Products</h1></div><a class="primary" href="{{ route('admin.products.create') }}">+ Add product</a></header><section class="admin-section"><table><thead><tr><th>Product</th><th>Collection</th><th>Price</th><th>Stock</th><th></th></tr></thead><tbody>@foreach($products as $product)<tr><td class="product-cell"><img src="{{ $product->image }}" alt=""><b>{{ $product->name }}</b></td><td>{{ $product->category->name }}</td><td>₹{{ number_format($product->price) }}</td><td>{{ $product->stock }} m</td><td class="actions"><a href="{{ route('admin.products.edit',$product) }}">Edit</a><form method="POST" action="{{ route('admin.products.destroy',$product) }}">@csrf @method('DELETE')<button onclick="return confirm('Remove this product?')">Delete</button></form></td></tr>@endforeach</tbody></table><div class="pages">{{ $products->links() }}</div></section>@endsection
