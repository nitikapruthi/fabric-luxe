@extends('layouts.store') @section('title','Checkout — Fabric Luxe') @section('content')<section class="checkout"><div><p class="eyebrow">Almost yours</p><h1>Checkout</h1>@if($errors->any())<div class="errors">Please complete the highlighted delivery details.</div>@endif<form method="POST" action="{{ route('checkout.place') }}" class="checkout-form">@csrf<label>Full name<input name="customer_name" value="{{ old('customer_name') }}" required></label><label>Email address<input type="email" name="email" value="{{ old('email') }}" required></label><label>Mobile number<input name="phone" value="{{ old('phone') }}" required></label><label class="full">Delivery address<textarea name="address" required>{{ old('address') }}</textarea></label><label>City<input name="city" value="{{ old('city') }}" required></label><label>PIN code<input name="postcode" value="{{ old('postcode') }}" required></label><fieldset class="full"><legend>Payment method</legend><label class="radio"><input type="radio" name="payment_method" value="cod" checked> Cash on delivery</label><label class="radio"><input type="radio" name="payment_method" value="upi"> UPI / prepaid</label></fieldset><button class="button full">Place order →</button></form></div><aside class="summary"><h3>Your order</h3>@php($subtotal=collect($items)->sum('line_total'))@foreach($items as $item)<p><span>{{ $item['name'] }} × {{ $item['quantity'] }}</span><b>₹{{ number_format($item['line_total']) }}</b></p>@endforeach<hr><p class="total"><span>Total</span><b>₹{{ number_format($subtotal+($subtotal>=999?0:99)) }}</b></p></aside></section>@endsection
