<article class="product-card"><a href="{{ route('product',$product) }}" class="product-image"><img src="{{ $product->image }}" alt="{{ $product->name }}">@if($product->is_new)<span>New</span>@endif</a><div class="product-info"><div><a href="{{ route('product',$product) }}"><h3>{{ $product->name }}</h3></a><p>{{ $product->material }} · {{ $product->color }}</p></div><div class="product-bottom"><strong>₹{{ number_format($product->price) }} <small>/ metre</small></strong><form method="POST" action="{{ route('cart.add',$product) }}">@csrf<button aria-label="Add {{ $product->name }} to bag">+</button></form></div></div></article>
