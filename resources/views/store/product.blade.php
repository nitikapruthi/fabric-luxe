@extends('layouts.store') @section('title',$product->name.' — Fabric Luxe') @section('content')<div class="breadcrumb"><a href="{{ route('shop') }}">Shop</a> / {{ $product->category->name }} / {{ $product->name }}</div><section class="product-page"><div class="product-large"><img src="{{ $product->image }}" alt="{{ $product->name }}"></div><div class="product-detail"><p class="eyebrow">{{ $product->category->name }}</p><h1>{{ $product->name }}</h1><div class="price">₹{{ number_format($product->price) }} <small>/ metre</small> @if($product->compare_at_price)<del>₹{{ number_format($product->compare_at_price) }}</del>@endif</div><p class="description">{{ $product->description }}</p><dl><div><dt>Material</dt><dd>{{ $product->material }}</dd></div><div><dt>Colour</dt><dd>{{ $product->color }}</dd></div><div><dt>Width</dt><dd>{{ $product->width }}</dd></div></dl><form class="purchase" method="POST" action="{{ route('cart.add',$product) }}">@csrf<label>Metres<input type="number" name="quantity" value="1" min="1" max="{{ $product->stock }}"></label><button class="button">Add to bag <span>→</span></button></form><p class="stock">● In stock · cut to order</p></div></section>@endsection
